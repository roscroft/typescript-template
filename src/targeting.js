/** @param {NS} ns */
export async function main(ns) {

}

// Project: get the best server to go after
// The best server to go after is the one that lets us reach our goals in the shortest amount of time
// The server that does this is the one that <1> maximizes the money we earn in a given time block
// That time block is the <2> time between augmentation installs

// In other words (more directly), amortize money per second over the time it will take to prep a 
// server, and ignore anything with a turnaround of more than a day or so

// Given a list of hosts and a current # of threads, 
// return a list of pairs of hosts and how long it'll take 
export function time_to_value(ns, hosts, threads) {

}