/** @param {NS} ns */
export async function main(ns) {

    ns.tprint()
}

function getType(contract, host) {
    return ns.codingcontract.getContractType(contract, host)
}

function parser(contract, host) {
    let contract_type = getType(contract, host)
    if (contract_type == "Find All Valid Math Expressions") {
        let answer 
    }
}

function allValidFns(data) {
    return
}